#ifndef HEAP_SORT_H
#define HEAP_SORT_H

/**
 * heap_sort: Sorts an array in ascending order, using heap sort algo.
 * 
 * data[]- The array to be sorted.
 * size- The number of elements in the array.
*/
void heap_sort(int data[], int size);

#endif